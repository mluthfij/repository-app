<!DOCTYPE html>
<html>
  <head>
    <title>Repository</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="d-flex flex-column <%= cookies[:moon] ? "text-white" : "text-dark" %>" id="<%= cookies[:moon] ? "moon" : "sun" %>">
    <%= render "layouts/components/navbar" %>
    <%= render "layouts/components/alert" if notice || alert %>
    <div id="page-content">
    
      <div class="container-fluid my-4">
      <%#  %>
        <%# <div id="context-menu">
          <div class="item">View</div>
        </div> %>
        <ul class="list-group" id="context-menu">
          <li class="item list-group-item list-group-item-action">An item</li>
          <li class="item list-group-item list-group-item-action">An item</li>
          <li class="item list-group-item list-group-item-action">An item</li>
        </ul>
      <%#  %>
        <%= yield %>
      </div>
    </div>
    <%= render "layouts/components/footer" %>
  </body>
</html>
