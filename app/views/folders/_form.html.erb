<%= form_for([@repo, @repo.folders.build]) do |f| %>
    <% if !subfolder.nil? %>
        <%= f.text_field :subfolder_id, value: subfolder.id %>
    <% end %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.hidden_field :private_role, value: @repo.private_role %>

    <p>
        <%= f.label :name, 'Folder name*', class:"form-label" %>
        <%= f.text_field :name, class:"form-control", required: true %>    
    </p>
    <p>
        <%= f.submit 'Create folder', class:"btn btn-success" %>
    </p>
<% end %>